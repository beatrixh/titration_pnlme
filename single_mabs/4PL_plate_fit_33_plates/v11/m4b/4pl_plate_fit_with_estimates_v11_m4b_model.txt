DESCRIPTION:

[LONGITUDINAL]
input={f, h, L_gap, eta_alpha, concentration, plate_row, plate_col, c_m4b, cc_mean, a_m4b}

concentration = {use=regressor}
plate_row = {use=regressor}
plate_col = {use=regressor}
c_m4b = {use=regressor}
cc_mean = {use=regressor}
a_m4b = {use=regressor}

PK:

EQUATION:
alpha = a_m4b + eta_alpha

E_ij = 1 - alpha*(
	exp(-plate_row) + exp(-(9 - plate_row)) + exp(-plate_col) + exp(-(13-plate_col))
	- 2*exp(-4.5) - 2*exp(-6.5)
)

L = cc_mean + exp(L_gap)

RLU_vc = c_m4b*E_ij
RLU_cc = cc_mean
RLU_sample = L + (c_m4b-L)/(1 + exp(h*(log(concentration) - log(f))))*E_ij

OUTPUT: 
output = {RLU_cc, RLU_vc, RLU_sample}