DESCRIPTION:

[LONGITUDINAL]
input={f, h, eta_alpha, eta_U, eta_L, plate_row, plate_col, U_estimate, L_estimate, alpha_estimate}

plate_row = {use=regressor}
plate_col = {use=regressor}
U_estimate = {use=regressor}
L_estimate = {use=regressor}
alpha_estimate = {use=regressor}

PK:

EQUATION:
alpha = alpha_estimate + eta_alpha
U = U_estimate + eta_U
L = U_estimate + eta_L

E_ij = 1 - alpha*(
	exp(-plate_row) + exp(-(9 - plate_row)) + exp(-plate_col) + exp(-(13-plate_col))
	- 2*exp(-4.5) - 2*exp(-6.5)
)

RLU_vc = U_estimate*E_ij
RLU_cc = L_estimate
RLU_sample = (L + (U-L)/(1 + exp(h*(log(t) - log(f)))))*E_ij

OUTPUT: 
output = {RLU_cc, RLU_vc, RLU_sample}