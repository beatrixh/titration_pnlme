DESCRIPTION:

[LONGITUDINAL]
input={L_gap, 
	m, 
	e, 
	alpha, 
	k, 
	plate_col, 
	plate_row, 
	mean_vc, 
	mean_cc, 
	binary_vc,
	binary_cc,
	binary_sample
}
plate_col = {use=regressor}
plate_row = {use=regressor}
mean_cc = {use=regressor}
mean_vc = {use=regressor}
binary_vc = {use=regressor}
binary_cc = {use=regressor}
binary_sample = {use=regressor}

PK:

EQUATION:
multi_edge = alpha*(exp(-k*plate_row) + exp(-k*(9 - plate_row)) + exp(-k*plate_col) + exp(-k*(13-plate_col)))

U = mean_vc 
L = mean_cc + exp(L_gap)

f_sample = (L  + (U-L)/(1 + exp(m*(log(t) - log(e))))) 

f = binary_sample*f_sample + binary_cc*mean_cc + binary_vc*mean_vc 
f_edge = f*(1 - multi_edge) 

OUTPUT: output = {f_edge}

